# wercker.yml
 
box: wercker/nodejs
# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A step that executes `npm install` command
    - npm-install

    - script:
        name: bower install
        code: bower install

    # a step that test print in CI system √
    # - script:
    #    name: test stampa
    #    code: echo "Test della stampa"


    # this assumes that your test suite runs via `grunt test`[x]
    - grunt:
        tasks: test




    # - script:
        # make sure that bower is listed in your package.json [x]
        # use `npm install --save bower` to add it.
        # name: bower install
        # code: node_modules/bower/bin/bower install