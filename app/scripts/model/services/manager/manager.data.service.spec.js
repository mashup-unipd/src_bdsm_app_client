/**
 * Name: app/scripts/model/services/manager/manager.data.service.spec.js
 * Author: MashUp
 * Mail: info@mashup-unipd.it
 * Desc: unit test to the service that manage user request to back-end
 *
 * Modify
 * Version  Date        Author          Desc
 * ==========================================================
 * 0.0.1    2015-05-20  Tesser Paolo    code module
 * -----------------------------------------------------------
 *
 */


describe('Service: dataManagerService', function() {
	'use strict';

	var factory = undefined;

	// var localStorageService = undefined;
	// var $httpBackend = undefined;

	beforeEach(function () {

		angular.mock.module('app.manager.data.services.module');
		// angular.mock.module('app');

		angular.mock.inject(function () {

			// factory = _dataManagerService_;
			// $httpBackend = _$httpBackend_;

		});

	});

	////////////

	it('should have dataManagerService factory', function(){
		// TODO

	});

	it('should have dataManagerService factory', function(){
		// TODO

		// $httpBackend.flush();
	});



});